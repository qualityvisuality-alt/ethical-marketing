import React, { useState } from "react";
import { ChevronDown, ChevronRight } from "lucide-react";
import { ELEMENT_ICON } from "../lib/elementIcons";
const COLORS={water:"#5aa9e6",wood:"#7bc47f",fire:"#f0803c",earth:"#d9a24a",metal:"#aeb9cc"};
const Chip=({text,color})=><span className="font-body text-[11px] px-2.5 py-1 rounded-full" style={{background:`${color}12`,border:`1px solid ${color}33`,color:"#d8d4c6"}}>{text}</span>;
function Card({s,isEn}){const [open,setOpen]=useState(false);const c=COLORS[s.element];const Icon=ELEMENT_ICON[s.element];return <div className="panel stage-card" style={{borderColor:`${c}44`}}><button className="stage-summary" onClick={()=>setOpen(v=>!v)} aria-expanded={open}><span className="stage-icon" style={{borderColor:c,background:`${c}1a`,boxShadow:`0 0 18px ${c}44`}}><Icon size={21} style={{color:c}}/></span><span className="font-display stage-number" style={{color:`${c}66`}}>{s.n}</span><span className="stage-summary-copy"><span className="font-label" style={{color:"#efe9d8"}}>{s.title}</span><small>{s.question}</small></span><span className="stage-output" style={{background:`${c}1f`,borderColor:c,color:c}}>{s.output}</span><ChevronDown size={18} className="text-dim" style={{transform:open?"rotate(180deg)":"none",transition:".25s"}}/></button>{open&&<div className="stage-detail"><p className="font-body text-[13px] leading-relaxed text-dim mb-4">{s.intro}</p>{s.includes&&<div className="mb-4"><div className="font-label text-[11px] tracking-widest mb-2" style={{color:c}}>{s.includesTitle}</div><div className="flex flex-wrap gap-1.5">{s.includes.map((x,i)=><Chip key={i} text={x} color={c}/>)}</div></div>}{s.groups&&<div className="grid sm:grid-cols-2 gap-3 mb-4">{s.groups.map((g,i)=><div key={i} className="rounded-lg p-3" style={{background:"rgba(255,255,255,.02)",border:"1px solid rgba(230,198,122,.12)"}}><div className="font-label text-[11px] mb-2" style={{color:c}}>{g.label}</div><div className="flex flex-wrap gap-1.5">{g.items.map((x,j)=><Chip key={j} text={x} color={c}/>)}</div></div>)}</div>}{s.format&&<div className="rounded-lg p-3 mb-3" style={{background:`${c}0e`,border:`1px solid ${c}2e`}}><span className="font-label text-[10px] tracking-widest" style={{color:c}}>{isEn?"FORMAT":"ФОРМАТ"}</span><p className="font-body italic text-[12px] mt-1" style={{color:"#d8d4c6"}}>{s.format}</p></div>}{s.extra?.map((x,i)=><p key={i} className="font-body italic text-[12px] text-dim mt-2 pl-3" style={{borderLeft:`2px solid ${c}55`}}>{x}</p>)}<a href={`mailto:qualityvisuality@gmail.com?subject=${encodeURIComponent(`Quality Visuality · ${s.title}`)}`} className="btn-gold inline-flex items-center gap-2 px-5 py-2.5 mt-4 text-xs">{isEn?"Discover the value":"Дізнатись цінність"}<ChevronRight size={15}/></a></div>}</div>}
export default function StagesAccordion({stages,title}){const isEn=title.includes("STAGES");return <section id="stages" className="relative py-20 px-5"><div className="max-w-4xl mx-auto"><div className="flex items-center justify-center gap-4 mb-5 reveal"><span className="diamond"/><h2 className="font-label text-center tracking-[.14em] gold-gradient-text" style={{fontSize:"clamp(20px,3vw,30px)"}}>{title}</h2><span className="diamond"/></div><p className="text-center text-dim text-sm max-w-2xl mx-auto mb-12 reveal">{isEn?"From immersion to direction, manifestation, embodiment and system. Each stage can stand alone or become part of the whole.":"Від заглиблення — до напрямку, прояву, втілення та системи. Кожен етап може бути окремим рішенням або частиною цілого."}</p><div className="space-y-3">{stages.map(s=><Card key={s.n} s={s} isEn={isEn}/>)}</div></div></section>}
